import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flexDirection: 'row',
    marginBottom: 16,
    alignItems: 'center',
  },
  reloadWrapper: {
    marginLeft: 'auto',
  },
});
