import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    paddingTop: 16,
  },
  image: {
    aspectRatio: 1,
  },
  label: {
    marginBottom: 8,
  },
  text: {
    marginBottom: 16,
  },
});
